# OreSight — cumulative dependencies through Phase 7 (Backend API)
# Keep this list small and CPU-friendly.

# Phase 1 — data foundation
numpy>=1.26,<3
pandas>=2.1,<3
pyyaml>=6.0,<7
pytest>=8.0,<9

# Phase 4 — PULSE production forecasting
lightgbm>=4.0,<5

# Phase 5 — RISK + SHAP root-cause attribution
shap>=0.46,<1

# Phase 6 — NUDGE prescriptive optimization
pulp>=2.9,<3

# Phase 7 — Backend API
fastapi>=0.100,<1
uvicorn>=0.20,<1
pydantic>=2.0,<3
httpx>=0.24,<1

# Phase 8 — React Frontend (Node.js / npm — see frontend/package.json)
# react 18, vite 5, recharts 2, vitest 1, @testing-library/react 14
